--- ../ArcadeMatrix_RPi/src/core/registry.rs
+++ ../ArcadeMatrix_RPi/src/core/registry.rs
@@ -10,6 +10,8 @@
         category: "test",
         version: "1.0.0",
     },
     capabilities: crate::core::engine_contract::Capabilities { supports_128x32: true, supports_256x64: true, realtime: true, interruptible: true },
     requirements: crate::core::engine_contract::Requirements { needs_audio: false, needs_network: false, needs_sd: false },
-    schema: crate::core::engine_contract::ConfigSchema { fields: Vec::new() },
+    schema: crate::core::engine_contract::ConfigSchema { fields: Vec::new() }, // We will mock schema elsewhere
     factory: || Box::new(MockEngine),
 };
